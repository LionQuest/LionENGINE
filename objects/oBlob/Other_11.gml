/// @description Chasing State
sprite_index = spriteRolling;
hSpeed = lengthdir_x(spd, direction);
vSpeed = lengthdir_y(spd, direction);

if atkTrigger state = 4;



//Reset vars
chargingFrames = random_range(60, 120);
attackFrames = 60;
